{
  "name": "pigeonhub-push-worker",
  "version": "0.1.0",
  "private": true,
  "description": "PigeonHub production transport spike: Cloudflare Worker -> Google OAuth2 (JWT) -> FCM HTTP v1. Free plan only.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260910.1",
    "typescript": "^5.8.0",
    "wrangler": "^4.131.0"
  }
}
